// Auto-generated by sdk-mcp-gen — do not edit by hand
// API: CN Work Agent API
// package: client
export type Json = null | boolean | number | string | Json[] | { [k: string]: Json };
export interface ClientOptions { baseUrl?: string; fetchImpl?: typeof fetch }
export function createClient(opts: ClientOptions = {}) {
  const baseUrl = (opts.baseUrl || "").replace(/\/$/, "");
  const f = opts.fetchImpl || fetch;
  async function request(method: string, path: string, body?: unknown) {
    const res = await f(baseUrl + path, {
      method,
      headers: body ? { "content-type": "application/json" } : undefined,
      body: body ? JSON.stringify(body) : undefined,
    });
    if (!res.ok) throw new Error(method + " " + path + " -> " + res.status);
    const text = await res.text();
    return text ? JSON.parse(text) : null;
  }
  async function getHealth(args: Record<string, unknown> = {}) {
    let path = "/health";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function getReady(args: Record<string, unknown> = {}) {
    let path = "/ready";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function getMetrics(args: Record<string, unknown> = {}) {
    let path = "/metrics";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function getOpenApi(args: Record<string, unknown> = {}) {
    let path = "/openapi.json";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function getPlatforms(args: Record<string, unknown> = {}) {
    let path = "/v1/platforms";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function getConfig(args: Record<string, unknown> = {}) {
    let path = "/v1/config";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function postWebhookFeishu(args: Record<string, unknown> = {}) {
    let path = "/webhook/feishu";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    return request("POST", path, args);
  }
  async function postWebhookDingtalk(args: Record<string, unknown> = {}) {
    let path = "/webhook/dingtalk";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    return request("POST", path, args);
  }
  async function getWebhookWecom(args: Record<string, unknown> = {}) {
    let path = "/webhook/wecom";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function postWebhookWecom(args: Record<string, unknown> = {}) {
    let path = "/webhook/wecom";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    return request("POST", path, args);
  }
  async function listApprovals(args: Record<string, unknown> = {}) {
    let path = "/approvals";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function getApprovalsCsv(args: Record<string, unknown> = {}) {
    let path = "/v1/approvals.csv";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function getApprovalsMd(args: Record<string, unknown> = {}) {
    let path = "/v1/approvals.md";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function getApprovalsHtml(args: Record<string, unknown> = {}) {
    let path = "/v1/approvals.html";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function getApprovals(args: Record<string, unknown> = {}) {
    let path = "/v1/approvals";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function getApprovalCard(args: Record<string, unknown> = {}) {
    let path = "/v1/approvals/{id}/card";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function getApproval(args: Record<string, unknown> = {}) {
    let path = "/approvals/{id}";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function decideApprovalGet(args: Record<string, unknown> = {}) {
    let path = "/approvals/{id}/decide";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function decideApproval(args: Record<string, unknown> = {}) {
    let path = "/approvals/{id}/decide";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    return request("POST", path, args);
  }
  return {
    getHealth,
    getReady,
    getMetrics,
    getOpenApi,
    getPlatforms,
    getConfig,
    postWebhookFeishu,
    postWebhookDingtalk,
    getWebhookWecom,
    postWebhookWecom,
    listApprovals,
    getApprovalsCsv,
    getApprovalsMd,
    getApprovalsHtml,
    getApprovals,
    getApprovalCard,
    getApproval,
    decideApprovalGet,
    decideApproval,
  };
}
