# Auto-generated by sdk-mcp-gen — do not edit by hand
# API: CN Work Agent API
from __future__ import annotations

import json
import re
import urllib.error
import urllib.parse
import urllib.request
from typing import Any, Mapping, MutableMapping, Optional

__package_name__ = "client"

class Client:
    """Sync HTTP client stub generated from OpenAPI."""

    def __init__(self, base_url: str = "", opener: Any = None) -> None:
        self.base_url = (base_url or "").rstrip("/")
        self._opener = opener

    def _request(self, method: str, path: str, body: Any = None) -> Any:
        url = self.base_url + path
        data = None
        headers: dict[str, str] = {}
        if body is not None:
            data = json.dumps(body).encode("utf-8")
            headers["Content-Type"] = "application/json"
        req = urllib.request.Request(url, data=data, headers=headers, method=method)
        open_url = self._opener.open if self._opener is not None else urllib.request.urlopen
        try:
            with open_url(req) as res:
                text = res.read().decode("utf-8")
                return json.loads(text) if text else None
        except urllib.error.HTTPError as e:
            raise RuntimeError(f"{method} {path} -> {e.code}") from e

    def getHealth(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """Health check"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/health"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        q = {k: str(v) for k, v in args_map.items() if v is not None}
        qs = urllib.parse.urlencode(q)
        return self._request("GET", path + ("?" + qs if qs else ""))

    def getReady(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """Readiness (503 while shutting down)"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/ready"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        q = {k: str(v) for k, v in args_map.items() if v is not None}
        qs = urllib.parse.urlencode(q)
        return self._request("GET", path + ("?" + qs if qs else ""))

    def getMetrics(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """Prometheus metrics"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/metrics"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        q = {k: str(v) for k, v in args_map.items() if v is not None}
        qs = urllib.parse.urlencode(q)
        return self._request("GET", path + ("?" + qs if qs else ""))

    def getOpenApi(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """OpenAPI 3 document"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/openapi.json"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        q = {k: str(v) for k, v in args_map.items() if v is not None}
        qs = urllib.parse.urlencode(q)
        return self._request("GET", path + ("?" + qs if qs else ""))

    def getPlatforms(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """List configured IM platforms"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/v1/platforms"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        q = {k: str(v) for k, v in args_map.items() if v is not None}
        qs = urllib.parse.urlencode(q)
        return self._request("GET", path + ("?" + qs if qs else ""))

    def getConfig(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """Redacted runtime config"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/v1/config"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        q = {k: str(v) for k, v in args_map.items() if v is not None}
        qs = urllib.parse.urlencode(q)
        return self._request("GET", path + ("?" + qs if qs else ""))

    def postWebhookFeishu(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """Feishu webhook (URL verification + messages)"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/webhook/feishu"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        return self._request("POST", path, dict(args_map))

    def postWebhookDingtalk(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """DingTalk webhook"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/webhook/dingtalk"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        return self._request("POST", path, dict(args_map))

    def getWebhookWecom(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """WeCom URL verification (echostr)"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/webhook/wecom"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        q = {k: str(v) for k, v in args_map.items() if v is not None}
        qs = urllib.parse.urlencode(q)
        return self._request("GET", path + ("?" + qs if qs else ""))

    def postWebhookWecom(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """WeCom webhook message"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/webhook/wecom"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        return self._request("POST", path, dict(args_map))

    def listApprovals(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """List approvals"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/approvals"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        q = {k: str(v) for k, v in args_map.items() if v is not None}
        qs = urllib.parse.urlencode(q)
        return self._request("GET", path + ("?" + qs if qs else ""))

    def getApprovalsCsv(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """Approval audit CSV export"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/v1/approvals.csv"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        q = {k: str(v) for k, v in args_map.items() if v is not None}
        qs = urllib.parse.urlencode(q)
        return self._request("GET", path + ("?" + qs if qs else ""))

    def getApprovalsMd(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """Approval list Markdown (Feishu/WeCom docs)"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/v1/approvals.md"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        q = {k: str(v) for k, v in args_map.items() if v is not None}
        qs = urllib.parse.urlencode(q)
        return self._request("GET", path + ("?" + qs if qs else ""))

    def getApprovalsHtml(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """Approval list HTML (local demo)"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/v1/approvals.html"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        q = {k: str(v) for k, v in args_map.items() if v is not None}
        qs = urllib.parse.urlencode(q)
        return self._request("GET", path + ("?" + qs if qs else ""))

    def getApprovals(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """List approvals (json | csv | md | html)"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/v1/approvals"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        q = {k: str(v) for k, v in args_map.items() if v is not None}
        qs = urllib.parse.urlencode(q)
        return self._request("GET", path + ("?" + qs if qs else ""))

    def getApprovalCard(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """IM card payload for an approval"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/v1/approvals/{id}/card"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        q = {k: str(v) for k, v in args_map.items() if v is not None}
        qs = urllib.parse.urlencode(q)
        return self._request("GET", path + ("?" + qs if qs else ""))

    def getApproval(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """Get one approval"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/approvals/{id}"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        q = {k: str(v) for k, v in args_map.items() if v is not None}
        qs = urllib.parse.urlencode(q)
        return self._request("GET", path + ("?" + qs if qs else ""))

    def decideApprovalGet(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """Approve or reject (unsigned GET, demo IM cards)"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/approvals/{id}/decide"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        q = {k: str(v) for k, v in args_map.items() if v is not None}
        qs = urllib.parse.urlencode(q)
        return self._request("GET", path + ("?" + qs if qs else ""))

    def decideApproval(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """Approve or reject"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/approvals/{id}/decide"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        return self._request("POST", path, dict(args_map))


def create_client(base_url: str = "", opener: Any = None) -> Client:
    return Client(base_url=base_url, opener=opener)
